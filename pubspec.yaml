name: pokemon
description: Pokemon recruitment.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.1
  localstorage: ^4.0.0+1
  http: ^0.13.4
  json_annotation: ^4.4.0
  sqflite: ^2.0.0+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  json_serializable: ^6.1.3

flutter:
  uses-material-design: true

#  assets:
#    - assets/data.json
